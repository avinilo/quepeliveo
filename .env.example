# Variables de entorno para desarrollo y despliegue
# Copia este archivo a .env y rellena las claves.

# Clave de TMDb para uso en servidor (Vercel Functions)
# En producción, configura esta variable en Vercel (Project Settings → Environment Variables).
TMDB_API_KEY=

# Clave de TMDb para uso en cliente (solo si NO usas proxy)
# En desarrollo, puedes ponerla para pruebas locales.
# Si usas el proxy /api/tmdb, NO es necesario exponerla al cliente.
VITE_TMDB_API_KEY=

# Token de acceso (TMDb v4) opcional para cabecera Authorization
# Solo si decides usar Bearer en lugar de api_key de v3
TMDB_ACCESS_TOKEN=

# Opcional: controlar uso de proxy en cliente (por ahora siempre activo)
# VITE_USE_TMDB_PROXY=true